# SpringBootFlywayTest
attemt to make web service

How to use:
Run postgres DB.
Edit application.properties correspondig to your DB's config.
Run project main.
Use swagger-ui (http://localhost:8080/swagger-ui.html by default)
